/**
 * V2 — On the Desk (placeholder)
 * Photoreal orbit + real repatching — coming soon.
 */
const app = document.getElementById('app');
if (app) {
  app.textContent = 'V2 On the Desk — coming soon';
  app.style.fontSize = '1.25rem';
  app.style.opacity = '0.85';
}
