/**
 * V4 — Night Mode (placeholder)
 * Neon + sequencer — coming soon.
 */
const app = document.getElementById('app');
if (app) {
  app.textContent = 'V4 Night Mode — coming soon';
  app.style.fontSize = '1.25rem';
  app.style.opacity = '0.85';
}
