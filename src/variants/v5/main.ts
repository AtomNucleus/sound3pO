/**
 * V5 — Exploded (placeholder)
 * Pull it apart — coming soon.
 */
const app = document.getElementById('app');
if (app) {
  app.textContent = 'V5 Exploded — coming soon';
  app.style.fontSize = '1.25rem';
  app.style.opacity = '0.85';
}
