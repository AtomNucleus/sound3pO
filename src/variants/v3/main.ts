/**
 * V3 — Toy (placeholder)
 * Clay & bouncy — coming soon.
 */
const app = document.getElementById('app');
if (app) {
  app.textContent = 'V3 Toy — coming soon';
  app.style.fontSize = '1.25rem';
  app.style.opacity = '0.85';
}
